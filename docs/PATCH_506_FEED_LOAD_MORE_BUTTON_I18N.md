# Patch 506 - Feed Load More Button i18n + polish

- Translates the feed load-more button through `ui.feed_load_more_posts`.
- Adds localized loading / retry labels via data attributes.
- Styles the button to fit the HNT app palette instead of the oversized old Socialite pill.
- Bumps the HNT app palette CSS version to v=506.
